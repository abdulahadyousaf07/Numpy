import numpy as np

# To make a sequence in arrays we use arange function
# It return the Numpy array
# Syntax = arange(start , stop , step)
# It is same as range in python

sequenced_array = np.arange(1,10,2)
print(sequenced_array)